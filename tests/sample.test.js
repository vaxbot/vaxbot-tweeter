//~ import {function1, function2} from '../index.js'; // these are just placeholder function names.
//~ const sample_data = require('./sample_data.js');

//~ test('it can read the name property of some sample data', () => {
  //~ expect(function1(sample_data[0].name)).toBeTruthy; // fill this in with real function calls
//~ });
